## What does this PR do?

<!-- One or two sentences. What changed, and why. -->

## Which package(s)?

<!-- e.g. @aidex/core, @aidex/providers. Keep PRs scoped to one concern where possible. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] Tests added/updated for the behavior this PR changes
- [ ] If this touches `packages/core`'s public API, lifecycle ordering, or
      another already-recorded architectural decision: a new ADR is included
      under `docs/decisions/` (see
      [CONTRIBUTING.md](../CONTRIBUTING.md#making-a-change))
- [ ] If this touches an architecture doc (`docs/architecture/`): the doc now matches
      the code exactly, with no drift in either direction
- [ ] No application-specific code (a specific product, tenant, or AI vendor
      hardcoded) was added to `packages/core`

## Related issue(s)

<!-- Closes #... / Relates to #... -->
